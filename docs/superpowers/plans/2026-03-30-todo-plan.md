# Todo List 實作計劃

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立極簡 Todo List，驗證 superpowers 開發流程

**Architecture:** 純前端 HTML/JS + localStorage，無後端。直接用瀏覽器打開 index.html運行。

**Tech Stack:** HTML5, JavaScript (ES6), CSS3, localStorage API

---

## 檔案結構

```
superpowers_todo/
├── index.html      # 主頁面
├── app.js          # CRUD邏輯 + localStorage
├── style.css       # 樸素樣式
└── docs/
    └── superpowers/
        ├── specs/
        │   └── 2026-03-30-todo-design.md
        └── plans/
            └── 2026-03-30-todo-plan.md
```

---

### Task 1: 建立 HTML 基礎結構

**Files:**
- Create: `index.html`

- [ ] **Step 1: 建立 index.html骨架**

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>
        <h1>Todo List</h1>
        <input type="text" id="todo-input" placeholder="新增 todo...">
        <button id="add-btn">新增</button>
    </header>
    <ul id="todo-list"></ul>
    <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: 手動驗證**

用瀏覽器打開 `index.html`，確認：
- 標題 "Todo List" 可見
- 輸入框和新增按鈕可見
- 空的 todo-list區域存在

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat(html): 建立基礎 HTML結構"
```

---

### Task 2: 建立 CSS 樣式

**Files:**
- Create: `style.css`

- [ ] **Step 1: 建立基本樣式**

```css
body {
    font-family: sans-serif;
    max-width: 600px;
    margin: 0 auto;
    padding: 20px;
}

header {
    display: flex;
    gap: 10px;
    align-items: center;
    margin-bottom: 20px;
}

#todo-input {
    flex: 1;
    padding: 8px;
}

#add-btn {
    padding: 8px 16px;
}

#todo-list {
    list-style: none;
    padding: 0;
}

#todo-list li {
    display: flex;
    gap: 10px;
    align-items: center;
    padding: 10px;
    border-bottom: 1px solid #eee;
}

#todo-list li.completed span {
    color: #999;
    text-decoration: line-through;
}

#todo-list li button.delete-btn {
    margin-left: auto;
    color: red;
}
```

- [ ] **Step 2: 手動驗證**

用瀏覽器打開 `index.html`，確認：
- header水平排列
- 輸入框和按鈕有適當間距
- (列表尚未有項目，無法驗證 completed樣式)

- [ ] **Step 3: Commit**

```bash
git add style.css
git commit -m "feat(css): 建立基本樣式"
```

---

### Task 3: 建立 localStorage 操作函數

**Files:**
- Create: `app.js`

- [ ] **Step 1: 建立資料結構與 localStorage 函數**

```javascript
const STORAGE_KEY = 'todos';

function loadTodos() {
    try {
        const data = localStorage.getItem(STORAGE_KEY);
        return data ? JSON.parse(data) : [];
    } catch (e) {
        console.warn('localStorage 載入失敗:', e);
        return [];
    }
}

function saveTodos(todos) {
    try {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(todos));
    } catch (e) {
        console.warn('localStorage 儲存失敗:', e);
    }
}
```

- [ ] **Step 2: 手動驗證**

在瀏覽器 console 測試：
```js
loadTodos()  // 應返回 []
saveTodos([{id: 1, text: 'test', completed: false}])
loadTodos()  // 應返回 [{id: 1, text: 'test', completed: false}]
```

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat(js): 建立 localStorage 操作函數"
```

---

### Task 4: 實作 renderTodos

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 新增 renderTodos 函數**

```javascript
function renderTodos() {
    const list = document.getElementById('todo-list');
    const todos = loadTodos();
    
    list.innerHTML = '';
    
    todos.forEach(todo => {
        const li = document.createElement('li');
        li.className = todo.completed ? 'completed' : '';
        li.dataset.id = todo.id;
        
        const checkbox = document.createElement('input');
        checkbox.type = 'checkbox';
        checkbox.checked = todo.completed;
        
        const span = document.createElement('span');
        span.textContent = todo.text;
        
        const deleteBtn = document.createElement('button');
        deleteBtn.className = 'delete-btn';
        deleteBtn.textContent = '刪除';
        
        li.appendChild(checkbox);
        li.appendChild(span);
        li.appendChild(deleteBtn);
        list.appendChild(li);
    });
}
```

- [ ] **Step 2: 手動驗證**

在 console 測試：
```js
saveTodos([{id: Date.now(), text: '測試項目', completed: false}])
renderTodos()
```
確認列表出現 "測試項目"

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat(js): 實作 renderTodos"
```

---

### Task 5: 實作 addTodo

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 新增 addTodo 函數**

```javascript
function addTodo(text) {
    if (!text.trim()) return;
    
    const todos = loadTodos();
    const newTodo = {
        id: Date.now(),
        text: text.trim(),
        completed: false
    };
    
    todos.push(newTodo);
    saveTodos(todos);
    renderTodos();
}
```

- [ ] **Step 2: 手動驗證**

在 console 測試：
```js
addTodo('買牛奶')
addTodo('寫程式')
```
確認列表出現兩個項目，localStorage 有資料

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat(js): 實作 addTodo"
```

---

### Task 6: 實作 toggleTodo

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 新增 toggleTodo 函數**

```javascript
function toggleTodo(id) {
    const todos = loadTodos();
    const todo = todos.find(t => t.id === id);
    if (!todo) return;
    
    todo.completed = !todo.completed;
    saveTodos(todos);
    renderTodos();
}
```

- [ ] **Step 2: 手動驗證**

在 console 測試：
```js
const todoId = loadTodos()[0].id
toggleTodo(todoId)
```
確認該項目變成 completed樣式

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat(js): 實作 toggleTodo"
```

---

### Task 7: 實作 deleteTodo

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 新增 deleteTodo 函數**

```javascript
function deleteTodo(id) {
    const todos = loadTodos();
    const index = todos.findIndex(t => t.id === id);
    if (index === -1) return;
    
    todos.splice(index, 1);
    saveTodos(todos);
    renderTodos();
}
```

- [ ] **Step 2: 手動驗證**

在 console 測試：
```js
const todoId = loadTodos()[0].id
deleteTodo(todoId)
```
確認該項目從列表消失

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat(js): 實作 deleteTodo"
```

---

### Task 8: 初始化與事件綁定

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 新增初始化與事件綁定**

```javascript
document.addEventListener('DOMContentLoaded', () => {
    renderTodos();
    
    const input = document.getElementById('todo-input');
    const addBtn = document.getElementById('add-btn');
    const list = document.getElementById('todo-list');
    
    addBtn.addEventListener('click', () => {
        addTodo(input.value);
        input.value = '';
    });
    
    input.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') {
            addTodo(input.value);
            input.value = '';
        }
    });
    
    list.addEventListener('click', (e) => {
        const li = e.target.closest('li');
        if (!li) return;
        
        const id = parseInt(li.dataset.id);
        
        if (e.target.type === 'checkbox') {
            toggleTodo(id);
        } else if (e.target.classList.contains('delete-btn')) {
            deleteTodo(id);
        }
    });
});
```

- [ ] **Step 2: 手動驗證 (完整驗收)**

驗收檢查表：
1. 在輸入框輸入文字，點「新增」 → 項目出現
2. 在輸入框輸入文字，按 Enter → 項目出現
3. 勾選 checkbox → completed樣式變化
4. 按「刪除」 → 項目消失
5. 重新載入頁面 → todos保留

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat(js): 完成初始化與事件綁定，功能完整"
```

---

### Task 9: 清理 localStorage測試資料

**Files:**
- None

- [ ] **Step 1: 清理測試資料**

在 console執行：
```js
localStorage.removeItem('todos')
renderTodos()
```
確認列表為空，準備開始正式使用

- [ ] **Step 2: 最終驗收**

逐一執行驗收檢查表 1-5，全部通過即完成。

- [ ] **Step 3: 更新 README**

記錄 writing-plans 流程完成狀態。

---

## 成功標準

功能正常運作：
- 能在瀏覽器新增/完成/刪除 todo
- localStorage 持久化
- 重新載入頁面資料保留